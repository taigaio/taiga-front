tg-lightbox-close

form
    h2.title
    p.question
    p.subtitle
    p.replacement
    select.choices
    p.warning

    div.options
        button.btn-link.btn-cancel(
            variant="icon"
        )
            span(translate="COMMON.CANCEL")

        button.btn-small.btn-confirm(
            variant="destructive"
        )
            tg-svg(svg-icon="icon-trash")
            span(translate="COMMON.DELETE")

