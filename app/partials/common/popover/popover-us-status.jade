ul.popover.pop-status
    <% _.each(statuses, function(status) { %>
    li
        a(
            href=""
            class!="status <% if(currentStatus == status.id) { %> active-popover <% } %>"
            title!="<%- status.name %>"
            data-status-id!="<%- status.id %>"
        )
            | <%- status.name %>
    <% }); %>
