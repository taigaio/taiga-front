ul.popover.pop-role
    <% _.each(roles, function(role) { %>
    li
        a.role(href="", title!="<%- role.name %>", data-role-id!="<%- role.id %>")
            |<%- role.name %> (<%- role.points %>)
    <% }); %>
